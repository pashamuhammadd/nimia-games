// Placeholder — once you have a Supabase project and have applied the
// migrations in packages/db/migrations/, generate the real types with:
//
//   npx supabase gen types typescript --project-id <project-ref> > packages/db/src/types.ts
//
// Until then this stays untyped so the client factories in this package
// still compile and can be used (just without query-level type safety).
export type Database = any;
