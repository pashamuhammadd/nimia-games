# @nimia/ui

Komponen shadcn/ui bersama + design tokens brand Nimia Games (maroon `#2b0a1a`, crimson `#c1124d`, pink `#ff4d8d`) yang dipakai lintas `apps/*` supaya `studio`, `portfolio`, dan `www` konsisten secara visual.

Status: kosong (placeholder). Diisi di **Tahap 4** — setup shadcn/ui, shell dashboard studio, form Order Service. Lihat `docs/ARCHITECTURE.md`.
