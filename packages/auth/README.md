# @nimia/auth

Helper Supabase Auth: baca session, cek role (`admin` vs `client`), proteksi route client portal (`studio.nimiagames.com/client/[client_id]`) di level middleware + database (RLS), bukan cuma UI.

Status: kosong (placeholder). Diisi mulai **Tahap 4** (UI dasar + halaman login/register). Bergantung pada `@nimia/db` (Tahap 3) untuk skema `users`/`clients`.
